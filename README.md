## Contact Form

The portfolio contact form submits directly to FormSubmit and delivers messages to weslyjasper7@gmail.com.

The implementation uses a standard HTML form POST rather than an AJAX request so it works more reliably on deployed static hosting.

Important: the first live submission may trigger a FormSubmit activation email to that inbox. Delivery will only start after that activation link is confirmed.

